library vnl_ui;

// Preferred public entrypoint (VNL prefix).
//
// Keep `shadcn_flutter.dart` as the source-of-truth entrypoint to reduce merge
// conflicts when syncing from upstream forks.
export 'shadcn_flutter.dart';
